name: invest
description: A new Flutter package project.
publish_to: 'none'
version: 0.0.1
homepage:

environment:
  sdk: ">=2.15.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  base:
    path: ../base

  flutter:
    sdk: flutter

dependency_overrides:
  flavor:
    path: flavor

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 1.0.0

flutter:
  uses-material-design: true
  assets:
    - flavor/assets/images/
